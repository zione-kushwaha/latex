services:
  - type: web
    name: texlitecollab-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    region: oregon
    envVars:
      - key: RUST_LOG
        value: info
      - key: PORT
        value: 3001
    healthCheckPath: /health
